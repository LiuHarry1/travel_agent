# Core dependencies
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.10.0
pydantic-settings>=2.0.0
python-multipart>=0.0.6  # Required for file uploads

# Vector database
pymilvus>=2.3.0

# Embedding providers
openai>=1.0.0  # For Qwen and OpenAI embeddings
requests>=2.31.0  # For BGE API mode
# sentence-transformers>=2.2.0  # For BGE embeddings (optional, not needed for API-only usage)
# transformers>=4.30.0  # Alternative to sentence-transformers for BGE
# torch>=2.0.0  # Required if using transformers

# Document processing
python-docx>=1.1.0  # For DOCX files
pdfplumber>=0.10.0  # For PDF files (better than PyPDF2)
PyMuPDF>=1.23.0  # For PDF image extraction (optional but recommended)
beautifulsoup4>=4.12.0  # For HTML parsing
markdownify>=0.11.6  # For HTML to Markdown conversion
Pillow>=10.0.0  # For image processing

# Text chunking
langchain>=0.1.0  # For RecursiveCharacterTextSplitter
langchain-text-splitters>=0.0.1  # For RecursiveCharacterTextSplitter (newer versions)
tiktoken>=0.5.0  # For token counting

